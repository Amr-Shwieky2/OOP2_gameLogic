#pragma once

/**
 * Run a demonstration of the MultiMethodDispatcher capabilities
 * showing damage calculation, interaction system, AI communication,
 * and render order determination.
 */
void runDispatcherDemo();

/**
 * Run a test of the CollisionAdapter that bridges the old collision
 * system with the new MultiMethodDispatcher.
 */
void runCollisionAdapterTest();